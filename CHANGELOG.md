# Changelog

## v2.4.0
- BUG: Java 直连查询构造握手包时 `_pack_varint(-1)` 在 Python 中死循环(负数右移恒为负), TCP 连接成功后事件循环被永久占死. 现按原版客户端线格式将协议号 -1 编码为无符号 32 位(FF FF FF FF 0F)
- 新增直连 SRV 记录支持: 输入纯域名且未显式指定端口时, 先查询 `_minecraft._tcp.<host>` 的 SRV 记录(与原版客户端行为一致), 命中则按记录的 target:port 建立连接, 握手包仍携带用户输入的原始地址; 显式指定端口(如 `host:port`)时不查询 SRV
- 加固: 共享 HTTP 会话设置显式 User-Agent——Cloudflare 会按浏览器签名封禁(error 1010, 实测无 UA / Python-urllib 默认 UA 即触发 403), aiohttp 默认 UA 目前未被列入黑名单但存在被封风险

## v2.3.0
- 新增「头像负缓存时长」配置: 头像下载失败后在该时长内不再重复尝试下载(仅限制重试频率, 不影响旧缓存展示), 设为 0 关闭负缓存(每次查询都重试)
- 新增「输出模式」配置(image / text): text 纯文本模式跳过图片渲染与头像预取, 几乎零 CPU/内存/流量开销; 图片模式行为不变
- 新增「卡片高度模式」配置(auto / fixed): auto 高度随内容自适应(紧凑无留白); fixed 固定高度 720px(玩家少时下方补白, 图片尺寸统一)
- 性能: 渲染模板携带 Shiki 运行时跳过标记, AstrBot 不再向云端渲染服务注入 1.2MB 代码高亮运行时, 单次渲染上传体积约 1.3MB → 约 50KB, 上行显著减负
- 性能: 渲染+发送段加全局发送闸门串行执行, 群聊多人同时查询不再叠加渲染/上传开销; 云端渲染增加 60 秒超时兜底(AstrBot 云端渲染 POST 本身无超时), 超时自动回退纯文本, 不再无限等待
- 性能: 玩家头像并发下载上限 4 路(原 8 路并发); mcstatus.io API 查询复用全局共享 HTTP 会话
- 优化: 日志瘦身——不再格式化完整玩家样例与完整查询结果, 降低日志体积
- 优化: 基岩版查询(同步 socket)移入线程执行, 超时等待期间不再阻塞事件循环

## v2.2.0
- 新增离线玩家识别: 判定为离线玩家, 完全跳过头像下载——纯本地判定, 零网络请求, 不进负缓存, 渲染显示占位色块, 盗版服查询不再反复发起注定失败的头像请求
- 玩家头像失败负缓存从 30 分钟缩短到 10 分钟: 网络抖动/限流等偶发失败的玩家更快恢复重试

## v2.1.1
- 修复代理模式(Velocity)卡片查询延迟不染色的问题: 代理卡片上的延迟数字此前始终显示为绿色, 现按 <1 秒 绿 / 1~3 秒 金 / 3 秒以上 红 正确分级(普通 Java 版与基岩版卡片不受影响)

## v2.1.0
- 查询提速: Java 版改为 mcstatus.io API 与 TCP 直连并发竞速, 典型查询耗时从 3~7 秒降至 1~3 秒; 直连协议号 ≤ 0(代理服)时复用竞速中的 API 任务补全版本范围, 不再发起第二次 API 请求
- 玩家头像加载改进: 新增玩家头像预取, 插件侧并发下载(mc-heads → crafatar → minotar 三源自动回退)并以 data URI 内嵌到渲染图, 渲染服务器不再临时拉取 mc-heads, 避免限流/超时导致的头像缺失
- 新增头像/图标磁盘持久缓存: TTL 仅作为刷新周期——头像默认 12 小时(可配置), 过期后下次查询尝试重新下载, 下载失败自动沿用旧缓存, 缓存文件永不因过期删除; 重启不丢缓存, 命中缓存零开销
- 服务器图标缓存回退: 图标随每次查询自带最新值, 某次查询未返回图标时自动沿用上一次的图标(不再显示首字母占位块); 从未成功获取过图标的服务器才显示占位块
- 新增 `/motdr` 指令清空头像/图标缓存, 下次查询重新下载
- 网络优化: aiohttp 会话全局复用, 避免每次查询重建连接与 TLS 握手; 插件卸载时自动释放

## v2.0.1
- 按插件市场发布文档补全 metadata.yaml 可选字段: astrbot_version(>=4.17.0), social_link, tags, 并移除非官方平台 key wechatpadpro

## v2.0.0
- 视觉全面改版「方块世界」MC 原生像素风: 泥土纹理背景, GUI 浮雕面板, 物品栏格子槽, 聊天框式 MOTD, XP 条式玩家进度条, 卡片高度改为内容自适应
- 新增服务器图标展示(API 图标/直连 favicon 均支持), 无图标的服务器显示像素占位块
- 新增在线玩家头像昵称列表(最多 8 个, 超出显示 +N), 头像加载失败自动回退为「首字母 + 哈希色」占位块, 不影响卡片
- 新增查询延迟显示(绿/金/红三级配色), 页脚展示数据源与查询时间
- MOTD 渲染引擎重写为段式解析: 支持 § 颜色码, 粗体/斜体/下划线/删除线格式码, §r 重置, JSON 组件树样式继承, API 与直连两条查询路径渲染风格统一
- API 查询路径改用 motd.raw 组件树自渲染, 修复 MOTD 颜色丢失的问题
- 新增配置项: 显示服务器图标 / 显示在线玩家列表 / 显示查询延迟, 均可独立关闭
- 文本回复统一使用 Unicode emoji(🔍 查询中 / ✅ 成功 / ❌ 失败 / 🟢 在线 / 🟠 未启动 / 🌐 代理), 全平台统一稳定显示
- 所有文本回复强制 content 纯文本通道发送(规避 AstrBot 默认 markdown 通道在无权限机器人上发送失败, 纯文本排版丢失的问题)
- 新增统一降级发送层: 图片渲染失败自动回退文本卡片, 失败仅记日志不再阻塞主查询流程, 「查询中」提示发送失败也不阻塞主查询流程
- 优化文本回退卡片内容, 补齐 MOTD, 延迟, 玩家数等字段
- 代理卡片升级: 子服展示上限 6 → 9, 新增迷你进度条与三态指示(在线/未启动/查询失败)
- 新增插件 logo 与插件市场形象字段(display_name/short_desc/support_platforms)
- 渲染输出改为 PNG 无损格式(替代 JPEG 质量 40, 避免像素风细节被压缩涂抹)
- 图片尺寸精确贴合卡片内容(viewport meta 声明渲染宽度 1280px, 不再出现底部大面积背景留白)
- 卡片始终充满图片: 新增弹性拉伸布局, 内容很少(人数少/查询失败)时卡片也会撑满整个渲染区域, 同时兼容不支持 viewport 高度解析的旧版文转图服务
- 卡片加宽至 1280px 并整体放大约 20% 字号; 数字字体由 VT323 更换为宽体像素字体 Press Start 2P, 人数/延迟等关键数字更清晰易读

## v1.7.3
- 优化 Java 版直连查询的网络 I/O, 将同步 socket 替换为 asyncio 异步 TCP, 不再阻塞事件循环
- 修复代理服务器并发查询时事件循环被阻塞的问题, asyncio.gather 现在可以真正并发执行

## v1.7.2
- 修复 MOTD 文本过长导致布局错位的问题, 主卡片限4行, 代理卡片限2行, 子服卡片限单行, 超出显示省略号
- 修复代理直连查询时查询失败的子服被静默忽略的问题, 现在会显示连接错误信息

## v1.7.1
- 修复子服列表高度不统一的问题, 所有卡片等高显示
- 修复 velostat 子服 MOTD 无法显示的问题(字段名映射错误)

## v1.7.0
- 新增子服未启动状态识别, 支持 velostat `online` 字段判断子服是否在线
- 新增离线子服橙色主题标识(橙点, 橙框, 橙字), 与查询失败(暗红)区分
- 优化查询失败子服的视觉样式, 添加暗红边框, 灰名, 半透明效果
- 优化代理服务器卡片布局, 整体字号放大, 间距收紧, 提升可读性

## v1.6.0
- 新增代理服务器查询功能, 支持查询 Velocity 代理及其子服状态
- 新增 velostat 插件集成, 通过 HTTP API 获取子服信息(推荐)
- 新增子服直连查询模式, 支持手动配置子服地址列表
- 新增代理服务器状态卡片模板, 显示母服+子服列表
- 配置项支持条件显示, 根据查询类型动态显示相关配置
